# 阶段签署与工程交接

日期：2026-09-09。

## 当前范围

只允许在从稳定main独立创建的experiment/target-anchored-transfer-ablation分支实现、验收和执行纯合成L0。它不是PM2.5真实数据实验；不使用原始站点CSV、checkpoint或历史结果选择任何设置。

协议优先级：02的澄清与公平对照 > 03的TransFusion原式与数值约定 > 01的初稿。README给出状态，不改变数学定义。

## 已完成的主代理修订

- 保留96标签压力测试，同时加768全标签格，禁止把人为少标签收益冒称本项目完整标签优势。
- F组是适用边界诊断，不强制假设失效时风险必须变差。
- 固定输入/状态缩放、LOO近邻阈值、原始坐标偏差、全部随机调用顺序和空接纳源规则。
- 删除伪n_eff，用时间块数描述依赖而不虚报独立样本量。
- 明确最终估计是联合nuisance拟合，不是cross-fitting。
- 要求matched station-linear-head等价核验，不能把同一线性估计量换名宣称新算法。
- TransFusion保留原无截距版，并强制增加公平非惩罚截距版；所有竞争门对两版同时成立。
- 精确区分两步损失缩放、中间样本量加权w及目标残差回归；不削弱强对照。

## 工程要求

1. 实现者只写实验分支；理论分支与main不修改。
2. 先做纯合成单元测试和少量专用工程fixture，不运行30个正式种子的性能筛选。
3. 必须有独立实现的预测/目标值复算、KKT检查、lambda=0退化、station-linear-head等价性及DGP V/F条件验证。
4. 不隐式跳过失败的c、种子、基线或数据格；数值失败、资源失败与性能STOP分别记录。
5. 源代码、协议、依赖版本与运行参数提交后，clean tree才可正式运行；结果目录不得覆盖。
6. 输出逐配置与逐种子指标、选定c、预测、偏差诊断、求解证书、数据生成哈希及完整manifest；不把缓存的硬编码零计数冒称操作系统级数据访问审计。
7. 可在实验分支固定新增凸求解依赖；不修改main环境或安装包源码。依赖版本/许可证和实际求解器必须记录，先验证再执行。
8. 全部正式源数据边界继续封存。即使合成L0通过，也仅能开始制定真实消融协议，不能自动启动站点训练或确认。

## 审查与签署

独立reviewer（Herdr transfer_reviewer，Codex gpt-5.6-luna high）已给最终PASS，仅限纯合成L0实现；明确核对了paper_exact及独立非惩罚截距版、两版竞争门、c平局规则、整格工程失败、样本加权w、两阶段缩放与截距KKT。一次长时间审查被主代理中断后，reviewer基于已读文件直接返回最终意见，未要求以赞同作为答案。

主代理签署：GO进入独立实验分支的纯合成L0实现与工程验收。正式30种子运行必须在代码验收、版本冻结和clean-tree后进行；本次签署不代表创新性或性能通过，不授予任何真实数据使用权。
