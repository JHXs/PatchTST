# Round 14 跨城市泛化确认结论

日期：2026-09-14。结论：**两任务合取 PASS**（预注册 G1–G4 全部成立）。

## 1. 执行绑定

- 分支：`experiment/cross-city-generalization-ablation`
- 执行 commit：`71bb1d952a995900f7655894acec72211badc958`（正式预检要求 40 位 commit、三个实现文件被 HEAD 跟踪、工作树 clean，全部满足）
- 命令：

```bash
python run_cross_city_generalization.py --formal --device cuda:0 \
  --output-dir experiments/results/round14/cross_city_generalization
python summarize_cross_city_generalization.py \
  --input-dir experiments/results/round14/cross_city_generalization \
  --output-dir experiments/results/round14/cross_city_generalization_summary
```

- 规模：8 个 b1 中心站 × 2 任务 × 2 臂 × 5 种子（7001–7005）= 160 次训练；矩阵一次跑完，运行时长 23 分 28 秒。
- 数据访问证据：实际打开站点集合 = b0 ∪ b1 共 16 站（9017, 9020, 9021, 9024, 9025, 9026, 9028, 9030, 9032, 9033, 9038, 9039, 9040, 9045, 9046, 9047）；与 `{9022, 9027} ∪ b2 ∪ b3` 交集为空；b2/b3 从未打开。
- 全部 16 个 `run_metadata.json` 的 `code_commit` 均为上述 commit。

## 2. 结果（物理尺度 PM2.5 RMSE）

| 任务 | 池效应 | G1 门 | G2 配对 | G3 站点 | G4 块 | 任务判定 |
|---|---|---|---|---|---|---|
| 24→1 | **+2.2072%** | ≥1.0% ✓ | 40/40 ✓ | 8/8 ✓ | 5/5 ✓ | PASS |
| 168→6 | **+1.8224%** | ≥0.5% ✓ | 37/40 ✓ | 8/8 ✓ | 5/5 ✓ | PASS |

- 合取状态：`conjunctive_two_task_pass = true`，`status = PASS`，`artifact_validation = PASS`。
- 名义单侧符号尾概率：24→1 为 9.09e-13，168→6 为 9.73e-09；按协议仅作描述性诊断（同站重复种子与相邻块非独立）。
- 站级效应（24→1 / 168→6，%）：9020 1.211/0.434、9026 0.699/1.529、9032 1.128/1.531、9033 6.666/5.284、9038 4.381/3.558、9039 0.812/0.322、9045 1.162/1.352、9046 1.598/0.570。
- 168→6 的 3 个负配对均集中于单站单种子，未影响四个门。

## 3. 次要分析

- SMAPE：24→1 由 16.3305% 降至 16.2125%（−0.1179 pp）；168→6 由 29.3657% 降至 28.7707%（−0.5949 pp）。与北京 24→1 SMAPE 上升的情形不同，本轮两任务 SMAPE 均改善。
- 逐块、逐预测步、峰值（各站训练段 q90）与训练段分位分解见 `cross_city_generalization_summary/`：`block_metrics.csv`、`lead_metrics.csv`、`peak_metrics.csv`、`quantile_smape_decomposition.csv`。
- 全部次要分析均为描述性，未参与放行。

## 4. 主代理独立复算

从 `24h_1h/station_9020/predictions/*_seed7001.npz` 直接重算：`base_rmse=6.161746`、`st_rmse=6.095509`、降幅 `1.074972%`，与 `paired_effects.csv` 中对应行完全一致（差值 <1e-9）。汇总器本身也从产物独立重建指标、时间戳、lead 与五块标签，并复算配置指纹。

## 5. 产物与归档

- 摘要与表格（提交入库）：`experiments/results/round14/cross_city_generalization_summary/`。
- 完整产物（含 checkpoints、predictions、逐站 metadata、训练日志，约 43 MB）归档：

```text
/home/hansel/.herdr/artifacts/PatchTST/round14-cross-city-generalization-71bb1d9.tar.zst
SHA-256: 41be51f36092b58322455fb9848ca8140a676cb51c170bba6a18c893c1aa3e5e
size: 13607247 bytes
```

## 6. 允许的申报与边界

**允许**：“固定结构在跨城市目标域广州的 8 个未消费中心站、5 个新种子与从未用于结构选择的 C-confirm 时间段上，相对同种子退化 PatchTST 稳定改善 24→1 与 168→6 的 PM2.5 RMSE（平均配对降幅 2.2072% 与 1.8224%，8/8 站与 5/5 块方向一致）。”

**不得申报**：

- 不构成第二创新机制；本轮未新增任何机制，只是第一创新的外部有效性验证。
- 不得称为全新城市或完全盲测：广州 9022（历史单站流程）与 9027 前 60%（OARR）此前已被消费，本轮通过排除与封存保证独立性，但不等于全城市未见。
- 不得外推到其他城市、其他污染物或其他时间段；b2（7 站）与 b3（7 站）保留池未被使用。
- 不得声称所有指标提升；不得以 SMAPE 改善替代 RMSE 主门。

## 7. 资源消费记账（须同步索引）

本轮实际消费：广州 b0（8 站，作邻站输入）、b1（8 站，作确认中心）、对应 C-fit/C-val/C-confirm 时间片、种子 7001–7005。b2/b3 仍为已登记、未授权、未消费。
