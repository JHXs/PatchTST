# Round 14 主代理签署

日期：2026-09-13。对象：`docs/round14-跨城市泛化确认/00_边界与协议.md`（修订版）。

## 1. 审查状态

| 审查 | 文档 | 首轮 | 复核 |
|---|---|---|---|
| 资源与独立性 | `01_资源与独立性审查.md` | REVISE（8 条放行条件） | **PASS**（§6） |
| 统计与门控 | `02_统计与门控审查.md` | REVISE（A–D 替代文本） | **PASS**（§6） |

两份审查由不同代理独立完成，互相不得阅读对方文件；主代理已采纳全部意见并逐条落入协议修订。

## 2. 主代理核验

- 分池可复算：以盐值 `PatchTST-cross-city-generalization-2026-09-11-v1`、`salt|Guangzhou|<id>`、SHA-256 升序与 `rank mod 4` 独立复算，结果与协议 §3 表一致（b0/b1 各 8 站，b2/b3 各 7 站；30 站不含已消费的 9022、9027）。
- 授权记账：主仓库索引已新增 §4.3「已授权但未消费（round14 / PIVOT-B）」并登记 b0、b1、C-fit/C-val/C-confirm、种子 7001–7005；b2/b3 保持「已登记、未授权、未消费」；§4.2 原则句改为指向 §4.3。
- 主要终点唯一可复算：块 `SSE/n` → 五块 SSE 合并的配对 RMSE → `d[s,r]` → 40 配对等权池均值；G1–G4 为两任务八门合取，不做多重校正；符号检验限定为名义描述性诊断。
- 停止与失败规则：禁止结果驱动中期停止与分母缩减；区分可原样重跑的纯工程失败与必须重新冻结的修复。
- 措辞边界：统一为「跨城市目标域广州的未消费中心站池」，明确不是全新城市或完全盲测。

## 3. 签署

主代理签署：**理论阶段 PASS，授权进入实现与消融阶段。**

授权范围（严格限于）：

1. 从最新稳定 `main` 创建 `experiment/cross-city-generalization-ablation`；
2. 在该分支实现广州专用无泄漏运行器与测试，冻结 selected commit、C-fit/C-val 子划分、超参数、命令、结果目录与机器可读聚合公式；
3. 工程冒烟只用合成数据或 b0 伪中心；
4. 一次执行 8 站 × 2 任务 × 2 臂 × 5 种子的固定矩阵，并在分支内按工作流 §5.1 完成确认。

未授权：改结构、改 k、改阈值、改块数、换种子、打开 b2/b3、读 b1 的 C-confirm 做小规模检查、以及在首次真实数据访问前跳过运行器白名单与 fail-closed 断言。

## 4. 资源状态提示

首次真实数据访问（含 b0 作邻站输入）后，b0 即转为**已消费输入资源**；正式矩阵运行后 b1、对应 C 时间片与 7001–7005 全部转为**已消费**，须由索引同步更新。若在实现阶段发现 BUG 需要重新冻结，按工作流 §2.3 与 §4.4 处理，不得沿用本批已查看资源作独立确认。
