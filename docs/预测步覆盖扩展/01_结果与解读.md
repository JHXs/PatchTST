# 预测步覆盖扩展：结果与解读

日期：2026-09-16。分支：`experiment/horizon-coverage-ablation`。协议见同目录 `00_协议.md`。

## 1. 已完成的规模

- **北京 1013**：历史窗口 L ∈ {24, 48, 72, 168} × 预测步 H ∈ {1, 3, 6, 12, 24} = **20 个配置**
- 每个配置两个臂（`degraded_patchtst` ＝ PatchTST 基线；`st_sparse_station_bias_delta_forecast` ＝ 冻结空间结构，k=5）
- 种子 2047–2051（5 个），旧管线，测试划分
- 其中 `24→1`、`168→6` 复用主确认结果（3.1188% / 0.8220%），其余 18 个为本轮新跑
- 汇总脚本对本轮结果做了**独立复算**（从逐样本预测重算 RMSE 并与运行器记录比对，不一致即报错）

## 2. 主结果：ST 相对 PatchTST 的配对降幅（%，5 种子）

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 3.119\* | 2.863 | 2.921\* | 3.588 | **3.847** |
| **48** | **3.361** | 3.009 | 2.594 | 1.728 | 2.408 |
| **72** | 2.009 | 1.973 | 1.414 | 0.894 | −0.193 |
| **168** | 0.986 | 1.368 | 0.822\* | −0.088 | −0.241 |

（`*` ＝ 主确认结果；其余为本轮 5 种子新结果）

改善种子数：

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| 24 | 5 | 5 | 5 | 5 | 5 |
| 48 | 5 | 5 | 5 | 5 | 5 |
| 72 | 5 | 5 | 5 | 5 | **1** |
| 168 | 5 | 5 | 5 | **3** | **0** |

## 3. 结论

1. **短历史窗口下，增益随预测步增大**：L=24 从 H=1 的 3.12% 升到 H=24 的 3.85%（全部 5/5 种子）。这是本轮最支持的结论，也符合物理直觉——预测越远，局部持续性越弱，邻站信息越有用。
2. **历史越长，增益越小**：L=48 降到 2.4%–3.4%，L=72 降到 −0.19%–2.01%，L=168 只有 0.99%/1.37%/0.82%，并在 H≥12 变为负（H=12 仅 3/5 改善、H=24 为 0/5）。
3. **长历史＋长预测步是方法的边界**：L=72/168 配 H≥12 时，空间修正被验证集早停"关掉"——诊断量显示 α 停在初始值约 0.10、空间残差比仅 0.03 左右、best_epoch 只有 1–7；而 L=24/H=24 时 α 学到约 0.21、残差比 0.21、训练跑满。即：长历史已提供足够上下文，邻站修正无增量。

**因此论文中不可写"增益随预测步单调增大"**，应写成：

> 空间邻站信息的价值取决于历史窗口与预测步的匹配：短历史（L=24）时增益随预测步增大并在 H=24 达到 3.85%（5/5 种子）；历史越长增益越小，L=168 在 H≥12 时消失甚至轻微为负。

这属于**边界条件的诚实披露**，也是消融充分性的证据。

> **结论定位（2026-09-16 导师决定）**：本轮 20 个配置**全部测量、无配置挑选**，因此可直接作为**正式确认结论**使用，
> 无需再用新种子复跑。若论文中并列两城结果，仍需注意第 3 节所述"北京旧管线 vs 广州无泄漏管线"的协议差异（待定）。

## 4. 产物

- 明细表：`tables/horizon_coverage/H1_grid_detail.csv`（每配置的均值、标准差、改善种子数、α、残差比）
- 网格表：`H2_reduction_grid`、`H3_improved_seeds_grid`、`H4_patchtst_rmse_grid`、`H5_st_rmse_grid`
- 图：`figures/horizon_coverage/HF1_horizon_trend`（左：降幅随 H 变化，四个历史窗口；右：RMSE 水平）、`HF2_gain_heatmap`（降幅热图）
- 原始运行（53 MB，含逐种子预测与检查点）归档：
  `/home/hansel/.herdr/artifacts/PatchTST/horizon-coverage-beijing-2047-2051.tar.zst`
  SHA-256 `5632ba7866bb2d51311ad534621ad3672e0d16602e919d817e0ad9afaf1526ee`

## 5. 阶段 B：频域适配器（20 配置已全部完成）

在阶段 A 的冻结 ST 检查点之上训练适配器（`st_rfft` 固定三段 rFFT；`st_time` 等容量时域对照，仅内部记录）。

**ST+频域 相对 ST 的配对降幅（%，5 种子）**

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 0.141 | 0.232 | 0.671 | 0.812 | **1.764** |
| **48** | 0.386 | 0.593 | 0.837 | **1.378** | 1.306 |
| **72** | 0.357 | 0.437 | 0.529 | 0.686 | 0.963 |
| **168** | **1.048** | 0.463 | 0.371 | 0.187 | 0.466 |

**ST+频域 相对 PatchTST 的累计降幅（%）**

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 3.256 | 3.089 | 3.572 | 4.370 | **5.543** |
| **48** | 3.733 | 3.585 | 3.411 | 3.082 | 3.685 |
| **72** | 2.359 | 2.402 | 1.936 | 1.573 | 0.772 |
| **168** | 2.024 | 1.824 | 1.190 | 0.097 | 0.227 |

**结论**

1. 频域适配器的增益同样**随预测步增大**（L=24：0.14% → 1.76%；L=48：0.39% → 1.31%），与空间结构的趋势一致；长历史时相反（L=168 从 1.05% 降到 0.19%）。
2. 全模型（ST+频域）相对 PatchTST 的累计增益最高出现在 **L=24/H=24：5.54%**，其次 L=48 各配置（3.1%–3.7%）。
3. **内部核对（不入论文图）**：频域相对等容量时域对照的差异在 20 个配置上全部落在 −0.31 ~ +0.10 个百分点，无系统性优势——与既有结论一致，即增益可归因于适配器容量而非频率结构。

产物：`tables/horizon_coverage/H6`（明细）与 `H7`（相对 ST）、`H8`（相对 PatchTST）、`H9`（相对时域对照）；图 `figures/horizon_coverage/HF3_frequency_trend`。

工程说明：阶段 B 期间发现其 npz 曾以标准化空间保存；汇总脚本已改为按 metadata 自动换算（独立复算仍与指标 CSV 一致），运行器亦已改为保存物理量。

## 6. 阶段 C：广州 8 站的跨预测步覆盖（20 配置已完成）

- 8 个 b1 中心站 × 20 配置 × 2 臂 × 5 种子 = **1600 次训练**，用时 3 小时 36 分；160/160 任务完成。
- 协议与 round14 确认一致：显式站点白名单（b0 ∪ b1）、因果填充（禁 bfill）、训练段筛站、两臂共享样本；
  汇总脚本从逐标量预测独立复算 RMSE 并与落盘的逐块 SSE 交叉核对。

**池化配对降幅（ST 相对 PatchTST，8 站 × 5 种子，%）**

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 2.207\* | **3.063** | 3.011 | 2.788 | 2.654 |
| **48** | 1.948 | 2.905 | 2.933 | 2.368 | 2.506 |
| **72** | 1.773 | 2.358 | 2.354 | 2.219 | 2.097 |
| **168** | 1.647 | 1.970 | 1.822\* | 1.831 | 1.398 |

（`*` ＝ round14 已确认的 24→1 与 168→6）

- **改善站点：8/8，全部 20 个配置**；改善配对 35–40 / 40。

**与北京对比**

| 方面 | 北京 1013 | 广州 8 站 |
|---|---|---|
| 增益范围 | −0.24% ~ 3.85% | **1.40% ~ 3.06%（全部为正）** |
| 最佳配置 | L=24/H=24（3.85%） | L=24/H=3（3.06%） |
| 长历史＋长预测步 | 失效（−0.24%） | 仍为正（1.40%） |
| 站点一致性 | 单站 | 8/8 站在每个配置上均改善 |

结论：**空间结构的增益在广州的表现更稳定**（不存在北京那种"长历史＋长预测步失效"的局部负值），但峰值略低；
两个城市的最佳历史窗口都是 **L=24**，说明短历史＋邻站信息是这套结构最匹配的设定。

## 7. 阶段 D：广州频域臂（20 配置已完成）

在阶段 C 的冻结 ST 检查点之上训练适配器（`st_rfft` 与等容量时域对照 `st_time`），
8 站 × 20 配置 × 5 种子 × 2 适配器 = **1600 次适配器训练**，用时 100 分钟。

**ST+频域 相对 ST 的配对降幅（%，8 站 × 5 种子池化）**

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 0.796 | 0.502 | 0.459 | 0.765 | **1.173** |
| **48** | 1.281 | 0.699 | 0.342 | 0.545 | 0.950 |
| **72** | 1.287 | 0.640 | 0.434 | 0.748 | 1.016 |
| **168** | **1.366** | 0.702 | 0.634 | 0.492 | 0.483 |

**ST+频域 相对 PatchTST 的累计降幅（%）**

| L \ H | 1 | 3 | 6 | 12 | 24 |
|---|---|---|---|---|---|
| **24** | 2.995 | 3.555 | 3.459 | 3.533 | **3.798** |
| **48** | **3.215** | 3.588 | 3.266 | 2.900 | 3.432 |
| **72** | 3.041 | 2.982 | 2.777 | 2.950 | 3.092 |
| **168** | 2.996 | 2.661 | 2.444 | 2.310 | 1.873 |

- 频域增益在 20 个配置上**全部为正**（0.34%–1.37%），改善配对 30–40 / 40；累计增益 1.87%–3.59%，跨配置非常稳定。
- **内部核对**：频域相对等容量时域对照在 18/20 个配置上为负（−0.03 ~ −0.50 pp），与北京结论一致——增益归因于适配器容量而非频率结构。

## 8. 三项贡献的完整覆盖矩阵

| 贡献 | 北京 1013（20 配置） | 广州 8 站（20 配置） |
|---|---|---|
| ① 空间结构 vs PatchTST | 3.85% ~ −0.24% | 1.40% ~ 3.06%（8/8 站全配置改善） |
| ② 跨城市泛化 | —（广州即泛化） | 已确认（round14）+ 本表 20 配置 |
| ③ 频域适配器 | 0.14% ~ 1.76% | 0.34% ~ 1.37%（全部为正） |

**跨城市一致的结论**：短历史（L=24）+ 中等偏长预测步是本结构最匹配的设定；长历史会削弱增益（北京在 L=168/H≥12 甚至转负，广州仍为正但明显下降）。

## 9. 尚未完成

- **阶段 B**：北京 1013 的频域臂（ST+频域、ST+时域对照）在上述 20 个配置上的适配器训练（约 200 次训练）。
- **阶段 C**：广州 8 站的同结构跨预测步覆盖（8 站 × 20 配置 × 2 臂 × 5 种子）。
- 其他模型家族对比（经典基线 / Informer / CT-PatchTST）：按用户指示放在覆盖扩展之后。
