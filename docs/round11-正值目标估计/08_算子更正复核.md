# 算子更正复核

结论：PASS

维度链条正确：B∈R^{n×n}，v=(I−B)r∈R^n；H_E∈R^{|E|×n}，所以 H_Eu、H_E(u−λv) 及 S_cv−S_rem=−λH_Ev 均为 |E| 维，H_E² 不再被使用。rho=||H_Ev||/(||H_Eu||+1e−12) 合法用于近零门。信息边界清楚：OOF query 只用 forward purge 后的早期训练 fit 记录，valid/test 不用评价响应，test 只用冻结训练记录。

放行条件：实现需屏蔽 excluded 行/列并固定列顺序，删除旧 H²/kappa 接口，禁止 query 回填 fit。“多数 lead”应在正式实验前预注册阈值；这不影响本次维度修复。
