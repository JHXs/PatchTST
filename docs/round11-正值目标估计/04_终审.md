# Round 11 CF-CV-CS 终审

## 裁决：REVISE

只改协议文字，不改 DGP/门槛；theory 不运行 fixture。

1. **预算。** 5-fold m-OOF 每个 seed/H/log 训练5个PatchTST，另需1个m_full。raw 不做 OOF 时，fixture=6+36=42，正式=100+600=700；raw 也做时为 72/1200。12/200漏算，须二选一。

2. **接口。** tsai PatchTST 的 c_out 仅兼容，输出按 c_in 保留通道；X=[B,3,L] 输出 [B,3,H]，不能配 y=[B,1,H]。最小修正是 X=[B,1,L]，仅输入 Y 历史，calendar 进 C；不得新增融合结构。

3. **时间戳。** 固定 lead h 时 t→t+h 一一对应，d_{s,h}=1；H=6 仅跨 lead 重复，而协议逐 lead。“去重权重”是恒等权重，应改称每 lead 权重 1；跨 lead 不得暗加权。

4. **选择。** selection_metric=valid_raw_MSE 违反“valid target 不选 m/超参”，并偏向 raw checkpoint。改为固定 epoch 或各 m-OOF fold 的 train-only blocked validation；raw/log 共用规则。smoother 选择可保留，但只用 train-inner validation，外层 valid/test 前冻结。

5. **边界。** theory 只留 Markdown；fixture 的代码、运行和日志须从稳定 main 独立建 experiment/round11 分支。fixture 通过后需显式授权 L0；不授权真实数据。

6. **可证伪性。** rho/kappa 加 removal 相对 5% 是必要可证伪门，但不足以单独归因。补逐 lead/query 断言：S_cv−S_rem=−lambda·H(I−B)r；两臂共享 m、C、fold、权重、smoother、边界和评价样本，并报告路径、协方差、伪结果方差及配对 RMSE。5% 门不变。

文字修复并独立检查后，才可 PASS 到非正式 fixture；不授权 L0。
