# 05 主代理签署复核

## 结论：PASS

六项均关闭：

1. 预算为 fixture 42 次、正式 700 次，含5-fold m-OOF、m_full、raw无 OOF。
2. 接口改为 X=[B,1,L]、y=[B,1,H]；日历不得进入模型，C 按 05 固定，无融合结构。
3. lead 权重为1，禁止跨 lead 暗加权。
4. checkpoint/smoother 只用 train-inner；outer valid/test 不选，raw/log 规则预注册。
5. theory 仅 Markdown；fixture 在独立 experiment/round11 分支，L0 仍需授权。
6. 已加入逐点恒等式 S_cv−S_rem=−λH(I−B)r、固定容差、配对 RMSE 记录，5% 门未降。

允许进入非正式 fixture；不授权正式 L0/真实数据。
