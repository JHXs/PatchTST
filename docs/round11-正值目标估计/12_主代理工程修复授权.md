# Round 11 主代理工程修复授权

日期：2026-09-10。状态：工程 STOP 后的阻断修复；主代理未查看任何 smoke 性能数值。本文件不改 DGP 或 5% 门。

## 冻结修复

1. 按理论 `09_前向冷启动样本空间更正.md` 实现 `M/S` 双池、`R` 选择、`B[nS,nM]`、逐 lead/逐 fit-prefix 的 `v0 -> v_tilde -> lambda`，以及 `H_E[|E_h|,|F_Eh|]`。早期 M 只能供 B 冷启动，不能进入最终 H 列。
2. 每个 raw/log full 或 log OOF fit 都只用自身 `inner_fit` 拟合输入 scaler 和目标 scaler；模型在标准化目标上训练，预测必须逆变换。不得共享完整 train scaler 给早期 OOF。
3. 条件量以 raw C 保存。每个因果 B/H OOF 行或块的条件 scaler 只能由其合法 fit prefix 拟合；最终 valid/test 的 H scaler只用完整训练 S。禁止未来 query 条件统计参与较早 OOF 距离。
4. `build_block_operator` 只能遍历 unique lead；加入与逐 lead 独立构造相等的测试。
5. conditional smoother 的 Gaussian local-linear bandwidth 固定为 1.0，不再运行伪“选择”。`SMOOTHER_MAX_SUPPORT=128` 在任何 3-seed fixture 性能被查看前冻结为资源上限；按距离取最近 128 时必须稳定排序，strongest/removal/candidate 完全共享。该值不是创新或调参证据，未来正式 L0 不得更换。
6. 主 fixture 只运行 heteroskedastic DGP。homoskedastic 和 additive raw 在本阶段必须有单序列、正值、log1p、跨 lead 唯一标签及 HPC/global 公式单测；不要求额外神经训练。工程记录须明确尚未执行负对照，所以 fixture 通过也不代表 L0-ready。
7. additive raw 的 log 目标必须是 `log1p(Y)`，不得保存 NaN 后声称支持 log 臂。
8. 每个 task sidecar 必须包含 schema、unit key、精确 7 fit keys 和 NPZ SHA-256。resume 在接纳“pair 已完成、manifest/progress 未登记”的 crash window 前必须校验 sidecar schema/key/fit keys/hash；增加只篡改 NPZ 后拒绝恢复的真实测试。
9. summarizer 必须从 seed 重建 DGP、split、OOF plan、M rows、C raw、B 支持、S selector、逐 lead中心/λ/H、global/HPC/factor 和全部 raw 预测公式；保存的神经 base predictions可作为冻结模型输出，但不得信任保存的 condition、factor、方法预测或 runner metrics。独立重算 42 fit keys，每 seed/task 恰为 7 且 log OOF blocks 为 0..4。
10. formal fixture 启动仍要求 clean tree、固定 branch/commit、固定 4001..4003、N=4096/burn-in=512、主 DGP和不存在的输出目录。修复阶段只允许单测和 `_smoke`，不允许 42 次正式 fixture。

所有 P0/P1 修复通过独立复核前，工程状态保持 STOP。
