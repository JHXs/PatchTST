# Round 11 工程终审

## 结论：PASS

- **M/S、冷启动、逐 lead：PASS。** S 只收同 lead、严格更早、purge 合法且支持≥2 的 M（`cf_cv_smearing_fixture.py:669-682`）；B 用 M、H 用 S；`v0/vtilde/lambda` 同一 lead/prefix 一致（`run_round11_cf_cv_fixture.py:159-186`、`170-176`、`203-212`）。
- **scaler/fairness：PASS。** 每个 fit 独立拟合 input/target scaler，按 lead 逆变换（`run_round11_cf_cv_fixture.py:21-31`、`114-145`）；B/H 的 C scaler 仅取合法 prefix（`cf_cv_smearing_fixture.py:538-583`）。
- **算子/support：PASS。** 仅遍历 unique lead（`cf_cv_smearing_fixture.py:468-498`）；128 已获 12 授权且稳定排序（`60-61`、`422-428`）；bandwidth 非 1.0 拒绝（`446-448`）。
- **summarizer：PASS。** 重建 seed、split、OOF、M/S、C、B/H、factor、raw 预测，仅信任神经 base output（`summarize_round11_cf_cv_fixture.py:181-345`）；独立核验 7 keys/task、总计 42（`348-395`）。
- **sidecar/resume/source：PASS。** sidecar 校验 schema、unit、7 keys、elapsed、NPZ SHA（`run_round11_cf_cv_fixture.py:279-295`）；resume 锁定 commit/branch/clean tree（`334-345`）；NPZ crash tamper 测试在 `test_round11_cf_cv_fixture.py:343-355`。
- **42/预算/语义：PASS。** 非 smoke 锁定 4001–4003、4096/512、40 epoch、patience=8（`run_round11_cf_cv_fixture.py:312-321`）；明确 registered 但 `formal=false/formal_l0=false`（`322-323`），fit keys 严格比较（`398-402`）。
- **smoke/rho、verification、负对照、历史 seed：PASS。** smoke 不 gate rho（`run_round11_cf_cv_fixture.py:404-410`）；verification 幂等且不改 runner manifest（`summarize_round11_cf_cv_fixture.py:167-178`）；负对照拒绝（`run_round11_cf_cv_fixture.py:306-307`）；历史 seed 交集拒绝并复核（`cf_cv_smearing_fixture.py:871-879`、`summarize_round11_cf_cv_fixture.py:365-371`）。
