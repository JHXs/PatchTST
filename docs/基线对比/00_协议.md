# 基线对比协议（草案 v1，待批准）

> 分支：`research/baseline-comparison-protocol`（从 `main@2a12fe8` 创建）
> 状态：**协议阶段**——只写 Markdown、只读代码；任何实现都必须在协议批准后从 `main` 另开实验分支。
> 一句话：把"我们的模型 vs 别人怎么做"用**与主实验完全相同**的数据、划分、种子、训练规则比一遍；
> 超参只许用验证集挑；**无论谁赢都如实报告**。

---

## 1. 这段对比要回答的四个问题

| # | 问题 | 为什么必须回答 |
|---|---|---|
| **Q1** | 同信息集下，我们的 Top-5 门控＋站点身份偏置，是否优于"把邻站当普通输入通道"的常规做法？ | 这是**空间贡献的立身之本**。若打不过"无门控的多通道 PatchTST"，则贡献①只能降级为"一种实现方式" |
| **Q2** | 相对单站 PatchTST 与常见深度学习时序模型（MLP/GRU/LSTM/TCN/ResNet/TST），我们处在什么水平？ | 论文表格主角；审稿人默认期待 |
| **Q3** | 相对不用深度学习的老方法（persistence／日周期／气候均值／AR／Ridge／线性空间回归）呢？ | 审稿人必问，**成本极低**（秒级），性价比最高 |
| **Q4** | 我们只有 21.2k 参数，是否"以小胜大"？ | 容量—性能关系是加分项；也是公平性答辩材料 |

**明确不回答的东西**：不比较训练时间、不比较推理成本、不比较多污染物/多站点联合训练——超出当前边界。

---

## 2. 基线清单（三层）

### A 层：传统／统计基线（CPU 秒级，无需 GPU）

| 编号 | 基线 | 输入（信息集） | 说明 |
|---|---|---|---|
| A1 | persistence | 中心站 t 时刻 PM2.5 | 直接复制最近观测；多步用同一值 |
| A2 | 日周期 naive | 中心站 昨天同一小时 | 季节性最朴素的对照 |
| A3 | 气候均值 | **仅训练段**同一（月×小时）均值 | 必须只用训练段统计量，禁用全序列均值 |
| A4 | AR(p) | 中心站自身 PM2.5 滞后 p 项 | 用 sklearn 线性回归拟合（本环境无 statsmodels） |
| A5 | Ridge | 中心站 12 特征 × 历史窗口展平 | 与 B 层同信息集 |
| A6 | 线性空间回归 | **中心 + Top-5 邻站**当前/滞后值 → 中心 PM2.5 | 与 C 层同信息集；线性上限参考 |

### B 层：单站神经基线（信息集 = 中心站 12 通道）
对上 `degraded_patchtst`（我们的自基线，同信息集、同主干）。

B1 MLP ／ B2 GRU ／ B3 LSTM ／ B4 TCN ／ B5 ResNet ／ B6 TST（均取 tsai 1.0.1 现成实现）

### C 层：多站神经基线（信息集 = 中心 + Top-5 = 72 通道）—— **最关键的一层**
对上我们的完整 ST 臂（贡献①）。

| 编号 | 基线 | 说明 |
|---|---|---|
| **C1** | **plain multivariate PatchTST**（72 通道进同一主干，**无门控、无差值、无站点偏置**） | Q1 的直接对手；这是"不做任何空间机制设计"的常规做法 |
| C2 | tsai TST／GRU 多通道版 | 换主干验证结论不依赖 PatchTST 骨干 |
| C3 | 线性空间回归（同 A6） | 线性参照 |

**关键：C 组必须拿到完全相同的 6 个站**（中心站 + 按无泄漏协议在训练段筛出的 Top-5 邻站），否则信息集不等价，比较无效。

---

## 3. 公平性六条（硬规则，不可协商）

| # | 规则 | 具体做法 |
|---|---|---|
| **R1** | **信息集对齐** | C 层与 ST 臂使用同一站点集合（训练段相关性筛出的 Top-5，配置级一致）；A/B/C 三层的输入通道逐条登记在表里 |
| **R2** | **容量两档并列** | 每个神经基线都报告参数量，并跑两档：(i) **库默认档**（防止"把基线做弱"的质疑）(ii) **同量级档**（≈20k 参数，与我们的 21.2k 对齐）。两档结果都进表 |
| **R3** | **训练规则一致** | 同优化器／学习率规则、同 epochs/patience、同 batch、同早停判据、同损失、同种子 → 唯一变量是模型结构 |
| **R4** | **调参只用验证集** | 超参搜索仅限验证划分，每族预算固定（≤ 4 组）；**测试集只在最终评估时使用一次**；搜索过程记录在产物里 |
| **R5** | **种子配对** | 与主实验同种子（北京 2047–2051、广州 7001–7005）→ 成对比较。基线是**新增对照臂**，不是新确认资源，不消耗封存种子 |
| **R6** | **指标与统计** | RMSE（主）／MAE／SMAPE；报告配对差与同向计数；未达档位要求时不做显著性宣称 |

**R7（诚实条款）**：若某基线打败我们的结构，**照实写**，并进入论文 Limitation；不得回头"调整协议"再比同一批结果。
若因此修改了我们的方法，则受影响的种子/配置标记为**已消费**，需要的确认必须换新种子——这条沿用主线既有规则。

---

## 4. 数据与协议（沿用主实验，不新增任何偏离）

| 项 | 北京（主） | 广州（泛化） |
|---|---|---|
| 管线 | **无泄漏**（训练段筛站 ＋ 因果前向填充） | **无泄漏** |
| 中心站 | 1013 | 8 个未消费站（round14 同一批） |
| 划分 | 70/10/20（时间顺序） | 同 round14（C-fit/C-val/C-confirm） |
| 评估划分 | test | confirm 段 |
| 目标 | PM2.5，单污染物 | 同 |
| 任务配置 | 20 个（L∈{24,48,72,168} × H∈{1,3,6,12,24}） | 同 |
| 报告单位 | 反标准化后的 μg/m³ | 同 |

---

## 5. 范围选项（**待你选定**）

| 方案 | 覆盖范围 | 机器时间（实测推算） | 论文可用性 |
|---|---|---|---|
| **A 紧凑** | 只在 2 个头条配置（24→1、168→6）× 5 种子 × 全部基线 | ≈ 1 小时 | 够用，但审稿人可问"为什么只在这两个任务上比" |
| **B 推荐** | 头条 2 配置 × 5 种子 **＋** 20 配置网格 × 3 种子 × 全部基线 | ≈ 3–4 小时 | 基线覆盖与主结果**同宽**，无法质疑"只挑赢的地方比" |

**成本依据（实测）**：北京无泄漏网格 20 配置 × 5 种子 × 2 臂 = 200 次训练耗时 **53 分钟** ≈ 16 s/次。
基线模型参数量与我们同量级（GRU 34k／LSTM 46k／TCN 69k），单次训练时间同量级。
**因此机器时间不是瓶颈**；两种方案的真实差别只在论文覆盖宽度。

**建议：选 B**。理由：多花 2–3 小时机器时间，换掉一个容易被质疑的点，且与已有的 20 配置主结果天然对齐。

**广州范围的建议**：不铺满 20 配置（8 站 × 20 配置 × 多样本会成倍膨胀），
只做 **头条 2 配置 × 8 站 × 3 种子**（≈ 48 次训练/基线臂）。

---

## 6. 门槛与验收

| 档位 | 要求 | 适用 |
|---|---|---|
| **低门槛投稿档**（工作流 §2.4） | ≥3 种子、平均改善 > 0、≥2/3 种子同向、1 名独立审查者 | 基线对比默认用此档 |
| **严格档**（默认） | 5 种子 ＋ 预注册门 ＋ 独立确认资源 | 仅当论文要把某条基线结论写成"确认级"时使用 |

**不预设"必须赢"**。基线对比的验收标准是**执行合规**（信息集、容量、调参、种子、统计全部合规），
不是"我们赢了"。若输，按 R7 处理。

**NC13 交叉引用**（工作流 §4.4）：基线对比本身不是新机制，不触发 NC13；
但 R1（同信息集）＋ R2（容量匹配对照）与 NC13 的精神一致，故本协议直接采用。

---

## 7. 禁止事项

1. 禁测试集调参、选模型、选结构、选配置。
2. 禁只跑一部分配置然后挑选有利的展示（B 档下必须全网格给数）。
3. 禁修剪、隐藏、改写不利结果。
4. 禁故意削弱基线：不给邻站信息、降低容量、缩短训练、关掉早停等，都算削弱；
   若出于研究目的需要削弱（例如"无邻站信息的消融"），必须**单独标注为削弱行**，不得混进主表。
5. 禁把"优于基线"写成无配对统计支持的强陈述。
6. 禁把 A1–A3 这类"无学习"基线的结果与我们的模型做"机制"层面的因果宣称。

---

## 8. 产物与命名

| 类型 | 路径 |
|---|---|
| 实现 | `baselines.py`（模型定义＋包装）、`run_baseline_comparison.py`（运行器）、`summarize_baselines.py`（独立复算）、`test_baselines.py`（测试） |
| 结果表 | `tables/baselines/`（B1 主表＝全基线逐配置；B2 容量对照；B3 配对差与同向计数；B4 传统基线明细） |
| 图 | `figures/baselines/`（BL-F1 主对比图、BL-F2 配置热图、BL-F3 容量—性能散点） |
| 文档 | `docs/基线对比/01_结果.md`（含执行合规自检：信息集/容量/调参/种子逐条打勾） |
| 归档 | `baselines-beijing-2047-2051.tar.zst`、`baselines-guangzhou-7001-7005.tar.zst` |

---

## 9. 资源与许可

| 项 | 内容 |
|---|---|
| 计算 | GPU AMD RX 7600（ROCm）＋ CPU；见 §5 估算 |
| 依赖 | 仅用**已有**依赖：tsai 1.0.1、scikit-learn、scipy、numpy、torch；**不新增第三方依赖**（本环境无 statsmodels/xgboost/lightgbm，故 AR/Ridge 用 sklearn 实现） |
| 数据 | 无新数据、无新站点 |
| 种子 | 与主实验配对（2047–2051／7001–7005）；**不消耗封存种子**（这是新增对照臂，非新确认资源） |
| 许可 | tsai（Apache-2.0）、scikit-learn（BSD-3）；无新增许可风险 |
| 已封存资源 | 广州 b2/b3 保留池**不动**；北京 1013 与广州 8 站仅复用已有划分 |

---

## 10. 本地实测事实（协议依据，2026-09-18 核验）

| 事实 | 数值 |
|---|---|
| 本模型参数量（`degraded_patchtst`） | **21,233** |
| 本模型参数量（`st_sparse_station_bias_delta_forecast`） | **21,234** |
| tsai MLP（12 通道默认） | 646,001 |
| tsai GRU（12 通道默认 h=100） | 34,301 |
| tsai LSTM（12 通道默认 h=100） | 45,701 |
| tsai TCN（12 通道默认） | 68,876 |
| tsai ResNet（12 通道默认） | 483,969 |
| tsai MLP（72 通道默认） | 1,366,001 |
| tsai GRU/LSTM/TCN/ResNet（72 通道默认） | 52,301 / 69,701 / 80,876 / 514,689 |
| 输入形状约定 | tsai 与我们的模型均接受 `[B, C, L]`，输出 `[B, horizon]` |
| 训练吞吐 | 北京无泄漏网格 200 次训练 / 53 分钟 ≈ **16 s/次** |
| tsai TST | 默认构造在 GPU 上有 device 不匹配问题，实现时需显式 `.to(device)`（已登记为实现风险，非阻塞） |

---

## 11. 待决事项（需用户/导师决定）

| # | 待决 | 选项 |
|---|---|---|
| **D1** | 范围 | **A 紧凑**（2 配置，≈1h）／**B 推荐**（2 配置 + 20 配置网格，≈3–4h）——协议建议 B |
| **D2** | 是否纳入广州基线 | 协议建议：做，但只做头条 2 配置 × 8 站 × 3 种子 |
| **D3** | 是否纳入仓库已有的 `Informer_model.py` / CT-PatchTST | 协议建议：**暂不纳入**（需移植到本协议管线，单独验证成本高；作为 future work） |
| **D4** | 是否需要把某条基线结论写成"确认级" | 默认否；若需要，另开严格档确认并把相关资源标记为已消费 |

---

## 12. 执行前提（批准后才做）

1. 协议批准（本文件冻结）。
2. 从**最新 `main`** 另开 `experiment/baseline-comparison-ablation`。
3. 先写 `test_baselines.py` 与实现工作单 → 独立代码审查 → 冒烟 → 正式矩阵。
4. 汇总脚本必须**独立复算**（不复用运行器中间量），并输出执行合规自检表。
5. 结果与结论提交前需 1 名不同族审查者（低门槛档要求）。
