# 基线对比 · 独立审查记录（P2）

> 审查者：`agy`（Antigravity CLI，Gemini 族）——与实现者 `codex`（GPT 族）**不同族**，满足工作流 §2.4 的独立性要求。
> 审查范围：分支 `experiment/baseline-comparison-ablation@de2acc3`（P1/P1B 实现 + 测试 + 冒烟）。
> 模式：只读（`--mode plan`），审查者被禁止修改仓库、禁止 push、禁止跑正式矩阵。
> 原始输出：`/tmp/baseline-review.log`（95 行；审查在 print 模式 5 分钟上限处截断，但结论与问题清单完整）。

## 1. 审查结论：**REVISE**（2 个阻塞 + 3 个次要）

判定依据（审查者原文）：
1. 阻塞——断点续跑对 `infeasible_oom` 任务失效（`run_baseline_comparison.py:247-248` 跳过条件同时要求 `.npz` 存在，
   而 OOM 任务从不生成 `.npz`）→ 续跑会重复执行并**重复追加** `raw_metrics.csv` 行。
2. 阻塞——`summarize_baselines.py:165-189` 的 `paired_table` 只算 `matched - default`，
   **未读取既有 ST 臂产物**，因此协议 §3 R8（best-of-baselines）与 R6（配对差/同向计数）没有实现，B3 表缺失。
3. 次要——`test_baselines.py:134-147` 的 T3 是**永真测试**：篡改发生在 `fit` 之后，且只比较 `predict(valid)`；
   审查者构造了一个"故意读测试集均值"的恶意 `fit` 函数，**T3 依然 PASS**。

次要问题另两条：T8 漏 `trad_climatology`（及 AR/Ridge）数值断言；传统基线行缺 `arm` 列（冒烟 CSV 实测为 NaN）。

## 2. 审查者已核实的合规项（不许在修复中改坏）

| 审查点 | 结论 | 审查者依据 |
|---|---|---|
| 信息集与泄漏（A 层） | **已核实** | 逐行核对 `baseline_traditional.py`：A1/A2 无拟合；A3 仅用训练段目标小时均值；A4/A5/A6 仅在训练段拟合；Ridge 的 alpha 只用 valid 从固定网格选取后在 train 重拟合；无 `bfill`、无全序列统计量；时间戳对齐实测 8759 小时间隔连续 |
| 训练语义一致性 | **已核实** | 逐条比对 `baseline_training.py::train_baseline` 与 `run_st_patchtst_ablation.py::train_one_run`（优化器/调度器/损失/裁剪/早停判据/checkpoint/最终评估划分） |
| C 层信息流 | 已核实（审查者独立跑扰动实验确认跨站臂确有信息流） | — |

## 3. 修复映射与证据（修复提交 `0c211ee`）

| 审查问题 | 修复动作 | 修复后实测证据 |
|---|---|---|
| 阻塞 1 续跑失效 | 不可行任务单独登记集合；`raw_metrics.csv` 写入幂等化；新增续跑测试 | 续跑 **0.2 秒**完成、CSV 哈希不变、重复行 0 |
| 阻塞 2 缺 R8/R6 | 新增 B3 best-of-baselines 配对（逐配置取全部同信息集臂最优 + `winner_arm` 标注 + 同向计数 + ST 缺失显式标 `missing`）；补合成测试验证"逐种子取最小"与配对方向 | 冒烟 B3：best 21.387 vs ST 20.988（配对差 −0.399 μg/m³，同向 1/1） |
| 次要 3 T3 永真 | 改为**拟合前**篡改测试集 + "访问即抛"受保护数据集；断言拟合期不读 valid/test；并自证"故意读 test 时 T3 会失败" | 恶意注入被 T3 捕获（记录见 `02_实现与验收记录.md` §9） |
| 次要 4 T8 缺断言 | 补 `trad_climatology` 小时均值数值断言；AR/Ridge 在已知线性关系数据上的数值检验 | 测试通过 |
| 次要 5 `arm` 列为空 | 传统臂行显式写入 `arm`；新增空值断言 | 冒烟 32 行、`arm` 空值 **0** |

修复后全量验证：**13/13 测试通过**（T1 位级门控差 `0.0`）；冒烟 32 行、重复行 0、独立复算最大相对差 `8.4e-16`。

## 4. 本次审查的方法论价值

T3 那条是本轮最有价值的发现：它说明**"测试全绿"不等于"测试有效"**。
若没有独立审查者用恶意注入反证，T3 会以"已覆盖防泄漏"的名义写进论文材料，
而实际上它连一个明显的泄漏都抓不到。后续阶段（P3/P4）的验收应沿用同样的"反证式"检查：
**先证明这道门能失败，再相信它的通过。**
