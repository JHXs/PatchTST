# Quality review

复核日期：2026-08-23。

## 独立reviewer大修追踪

| 发现 | 修订 |
|---|---|
| 接口隔离被误写为信息正交 | 改为轴/接口区分、互补待验；加入F00–F11 2×2及交互/衰减 |
| L0用ST残差可能stacking泄漏 | 改为直接目标的五折nested forward-chaining ridge；明确OOF备选边界 |
| L1缺self-history硬门与非结构容量对照 | B1改为3/3且≥0.2%；加入同输入B-flat；其架构和可执行参数匹配规则在第二轮复审中进一步冻结 |
| 广州确认不完整 | 冻结两任务、5种子、候选池含9058、不足5邻站、配对hash、30拟合预算 |
| 原创性过宽 | 正面对比TFT/Lag-FLSTM；限定为冻结ST残差适配实验方案 |
| 形状、组门、梯度不精确 | 区分外部/逻辑形状；固定全局softmax后组求和；纠正首步梯度 |
| 数据缺失率写未知 | 直接审计两个CSV，解析缺失/非有限为0；保留语义签字风险 |
| iScience计数/许可不精确 | 修正460配置/92组与CC BY 4.0 |
| 无泄漏条件不自动 | 加目标集合互斥、train-only筛站、禁bfill、fit范围ledger、冻结候选池 |
| shuffle与高污染门含糊 | 固定24起点块、5种子、置换对象/汇总；统一1.0%实际效应门 |
| 评分证据地位过高 | 加1/3/5锚点，轴/接口不再全5，声明仅决策辅助 |
| 支持文档结论冲突 | README、阶段门、evidence map、traceability、review与progress同步 |

## 第二轮复审三项修订

| 复审发现 | 修订 |
|---|---|
| 168→6按起点连续分块仍会共享5个目标时间戳 | 八块改称名义起点块；所有block、outer和inner边界从左侧删除`H−1`个起点，逐折自动断言目标时间集合交集为空；24→1删除0个 |
| M与B-flat结构/参数匹配不可复现 | 冻结M三类两层GELU网络、维度/偏置/dropout/初始化；冻结B-flat三层GELU旁路；`w=1…256`仅按参数数选，最小差上限3.0% |
| M-shuffle子集和种子汇总不明确 | 每块固定24预测起点；每个L1训练种子独立做5个置换；原始/置换在同一删尾子集重算；三个训练种子分别过50%门后再报均值，禁止合并预测 |

## 数据语义签字修订

| 审计发现 | 冻结处理 |
|---|---|
| 北京PM10连续1341 h为1000 | PM10两城全局排除 |
| 北京pressure存在约745–760与997–1008双量级 | `<900`固定乘1.33322，换算后验收`[900,1100] hPa` |
| 两城humidity零值；广州辅助污染物有非正值 | 按缺失处理，最多6 h因果ffill，残余缺失窗口删除 |
| wind_speed无单位字典且跨城离散模式不同 | wind_speed两城全局排除 |
| 精确来源与真实入库延迟无证明 | 仅称`timestamp<=t−1`历史回放，不称实时部署 |

研究负责人代理签字后，MVP冻结为`P=7`，M/B-flat及L0定义同步重算。该签字是工程可用性假设，不把未决血缘或延迟说成已证明。

## 当前风险

- 精确来源、既有填补原因、采样结束语义和真实报送延迟仍不可证；已通过历史回放表述边界管理，不能外推实时部署。
- L0线性筛查会漏掉纯非线性信号，这是预先接受的低成本保守止损风险。
- 实现必须复核理论参数数（M为1532/6547，B-flat为1532/6540）；若框架实计导致候选最小差超过3.0%，协议直接失败，不能用性能或无效参数调整。
- 预登记种子仍需跨分支人工核验未被消费。
- 北京测试不是未知；广州9022也有历史项目使用，只能作预指定迁移确认。

## 机械检查结果

- 21个Markdown文件：相对链接错误0；尾随空白/缺少末尾换行0。
- 17个深核验论文来源ID及数据语义权威来源、61个唯一外部URL的语法与交叉表述已复核；TFT、Lag-FLSTM、iScience、Microsoft数据来源推断、NIST/WMO单位和HJ 633边界均保留证据等级。
- `git diff --check`及对全部未跟踪Markdown逐文件的`--no-index --check`无空白错误。
- 关键词检查未发现TODO/TBD、旧“理论已放行/审查已通过”、未定义“显著恶化”、旧92组误写或B3严格等容量表述。
- `git status`只显示指定文档目录；HEAD仍为`1a26492`，无提交、无代码/结果文件。
- 数据语义重冻结后复算：`P_M`为1532/6547，B-flat最优宽度为15/47、参数为1532/6540、相对差0%/约0.1069%；均通过3.0%门，未来实现前仍须实计。
- embargo玩具索引检查确认：24→1删除0个、168→6删除5个左侧起点后，边界两侧目标时间集合交集均为0。
- 21个Markdown均执行相对链接、尾随空白、末尾换行和非Markdown产物检查；协议文档中的参数列表、检查清单和必要强调保留。

结论：数据语义代理签字与协议重冻结后，阶段门为“条件放行L0”；不是审查通过即性能放行，第二创新点仍未成立。
