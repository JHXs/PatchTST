# Revision notes

日期：2026-08-23。

- 本轮输入：独立 Codex reviewer “需大修”，12项P1/P2。
- 核心决策：撤回“接口隔离证明信息正交”和“理论已放行”；改为建模轴/工程接口区分，互补待2×2检验。
- L0决策：不用ST in-sample残差，采用直接目标nested forward-chaining ridge；L0只筛查数据。
- L1决策：新增B1硬门、同输入参数匹配B-flat；B3降为条件门消融。
- 数据事实：两个中心站各8760×14；时间戳解析/重复/断点0；原九候选解析缺失/非数值/非有限0，但存在PM10极端平台、pressure双单位量级、humidity零值和wind_speed跨城编码风险。
- 代理签字：验证前冻结`P=7`，排除PM10与wind_speed；固定pressure换算、零值缺失、最多6 h因果ffill和历史回放时间定义。
- 未决边界：精确来源、既有填补原因和真实报送延迟仍不可证；不阻止L0，但禁止实时部署与传感器因果表述。预登记种子仍须核验未消费。
- 最终门：数据语义代理签字与协议冻结已完成，条件放行L0；不是性能放行。
- 第二轮复审：168→6不能把连续预测起点块当作目标集合互斥；所有outer/inner边界固定删除左侧5个起点，24→1删除0个，并强制空交集断言。
- 架构冻结：`P=7`后M参数公式`529+1003H`；B-flat输入84维、固定三层MLP，宽度仅按`1…256`参数差选，差异上限3.0%；预计算宽度15/47。
- Shuffle冻结：每块24预测起点；每个L1训练种子独立5次置换，原始/置换同一删尾子集，三种子分别过50%门。
