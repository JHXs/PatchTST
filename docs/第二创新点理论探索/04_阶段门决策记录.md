# 第二创新点理论阶段门决策记录

- 决策与大修日期：**2026-08-23**
- 分支：`research/second-innovation-theory`
- 主候选：**MC-CVLRA**
- 备选：**RES-Peak**（只登记）
- 当前结论：**L1 正式选择 STOP；MC-CVLRA归档，不进入独立确认**

## 1. 对上一版结论的替代

独立 Codex reviewer 给出“需大修”。本记录正式替代上一版“理论阶段已放行”的措辞。当前没有模型实现、训练或测试结果；MC-CVLRA 不是已成立的第二创新点，也没有获得性能放行。

建模轴和工程接口可区分：第一创新点利用邻站 PM2.5，主候选利用中心站共污染物/气象历史并在冻结 ST 输出后适配。但接口隔离不能证明信息正交或互补；该问题已转化为 degraded PatchTST / degraded+adapter / locked ST / locked ST+adapter 的 2×2 因子比较。

## 2. 当前门状态

| 门 | 状态 | 依据或未决项 |
|---|---|---|
| 排除频域换名重复 | 满足 | 不做 FFT/VMD/wavelet/滤波/分解/多尺度卷积 |
| 与既有分支去重 | 满足 | 频域和 pollutant-specific alpha 失败已审计 |
| 建模轴/工程接口区分 | 满足 | 中心站跨变量历史、冻结 ST 后残差接口 |
| 信息互补 | **未通过预注册证明门** | F11相对F10平均改善0.477392%，低于0.5%；对B-flat为0/3且平均−0.161018% |
| 现有数据解析可达 | 满足 | 北京1013、广州9022均8760×14；原九候选解析缺失/非数值/非有限均0，但语义异常另审计 |
| 数据语义 | **代理签字完成** | `P=7`；排除PM10、wind_speed；pressure固定换算；humidity/辅助污染物零值按缺失 |
| 实时可用性 | 不作实证声称 | 仅按CSV时间戳定义历史回放；真实采样结束、入库和报送延迟无日志证明 |
| L0 协议无 stacking/边界泄漏 | **实现与复算通过** | 直接目标 nested forward-chaining；34个block/outer/inner边界目标时间集合均空交集，168→6 embargo=5、24→1=0 |
| L0 跨变量离散滞后信号 | **PASS** | 168→6 相对self/recent五折平均RMSE改善2.1784%/2.0274%，方向4/5与5/5 |
| 神经适配器正式选择 | **STOP** | F10、B1、B2、B-flat和M-shuffle硬门失败；完整性复算PASS |
| 原创性边界 | 已收紧 | 不是通用变量选择；TFT、Lag-FLSTM 为直接先例 |

## 3. L0 实验分支回填（2026-08-23）

L0 在 `experiment/cross-variable-lag-adapter-ablation` 分支只使用北京1013的train+validation选择区间执行。先只读8760个原始时间戳，精确建立6132/876/1752的70/10/20行边界；允许列只加载前7008行。test特征行、样本、预测、缓存和指标构造数全部为0，未实现或调用MC-CVLRA/ST神经模块。

168→6主任务的五折平均`RMSE_all`为：`R_self=42.092536`、`R_recent=42.027657`、`R_lag=41.175590`。`R_lag`相对self改善2.178407%，4/5折同向；相对recent改善2.027396%，5/5折同向。四项预注册硬门全部满足，唯一结论为**PASS**。

24→1支持任务的五折平均`RMSE_all`为：`R_self=18.179102`、`R_recent=17.861815`、`R_lag=17.801861`。相对self/recent改善2.075136%/0.335655%，方向为5/5与3/5；该任务不参与硬门。

独立复算从逐预测CSV重建逐折/逐horizon指标、配对差和gate，最大绝对数值误差不超过`2.14e-14`；34个目标时间边界和30个alpha选择组全部复核通过。首轮工程运行因浮点`0.7+0.1`截断导致选择止点误为7007，已判无效并保留说明；修复仅把冻结的70/10/20改为精确整数比例，没有改变变量、lag、alpha、Q/折或门槛。权威结果见[L0报告](06_L0选择区间筛查报告.md)与[结果目录](../../experiments/results/cross_variable_lag_adapter/l0/L0选择区间报告.md)。

## 4. L1正式选择与止损（2026-08-24）

冻结L1使用北京1013的train+validation区间、种子2060–2062和八个预注册变体。正式test六项访问计数均为0；独立复算完整性为PASS。

168→6中F11相对F10为3/3改善，平均0.477392%，低于0.5%门；相对B1平均仅0.073843%，相对B2平均0.022306%，相对B-flat为0/3且平均−0.161018%。M-shuffle中种子2061的增益损失仅19.1907%，低于50%。因此多个预注册硬门失败，唯一结论为**STOP**。24→1的3/3改善和高污染/逐horizon门通过不能替代失败的主门。

权威结论见[L1正式选择止损报告](08_L1正式选择止损报告.md)与[统一阶段门](../../experiments/results/cross_variable_lag_adapter/l1_selection_summary/gate_status.json)。不创建confirmation分支，不打开正式test，不进入广州迁移确认，也不扫描配置挽救。

## 5. 条件放行进入 L0 的两个前置条件（历史，已满足）

以下两项已经完成并留下记录，因此可从最新稳定 `main` 创建独立实验分支执行 L0：

1. **协议修订确认**：研究负责人代理逐项确认 [最终推荐方案](03_最终推荐方案.md) 中 outer/inner的`H−1`起点embargo、ridge 正则层级、M/B-flat固定架构与参数计数、2×2、逐训练种子M-shuffle、北京/广州确认和自动无泄漏断言均被冻结；不得在实验中后验解释或改阈值。
2. **数据语义代理签字**：已在验证结果出现前回到理论阶段，将输入重新冻结为七变量并固定确定性清洗。时间可用性操作定义为预测起点 `t` 只读CSV时间戳 `<=t−1` 的完整小时记录；因真实报送日志缺失，论文限于历史回放，不宣称实时部署。见[签字记录](05_数据语义审计与代理签字.md)。

解析完整性已直接审计，不再写“缺失率未知”。未来因果数据管线仍必须执行训练片缺失率 10% 门、禁止 bfill 和拟合范围日志。

## 6. 冻结的后续协议（历史）

- 北京任务：1013，168→6 主筛查/主选择，24→1 次任务。
- 广州迁移：9022，24→1 与168→6；候选池9017–9047加9058。9022历史上已用于单站流程，不称项目级未见盲测。
- 北京候选池1001–1036，明确含1036；所有筛站只接收训练片。
- 七变量、`K={0,1,2,3,6,12}`、原值+一阶变化、`d=16`、全局softmax后两组求和；M的投影、`q_h`和输出头均为固定两层GELU MLP、带偏置且`dropout=0`。
- beta有界残差、零初始化和显式旁路只作为安全机制。
- L0：八个名义起点块组成五个扩展窗口外层折；168→6在每个block、outer和inner边界从左侧删除5个起点，24→1删除0个；每折outer/inner目标时间集合交集必须为空。直接目标 ridge，固定 alpha 网格；主门只看168→6。
- L1：B0、B1、B2、B-flat、B3、M及完整2×2；B3仅为条件门消融。B-flat固定三层GELU MLP，宽度在1–256中仅按参数差确定，最小差须≤3.0%，否则协议失败。
- 高污染实际效应门统一为相对基线 RMSE 恶化≤1.0%。
- 固定 M-shuffle：每块恰含24个预测起点；每个L1训练种子在相同删尾诊断子集上重算原始与五个置换增益，三个训练种子分别须达到50%增益损失，禁止合并预测。
- 测试只在固定 commit 后一次打开，结果不得回流改结构。

## 7. 阶段路径与实际落点

```text
L0 PASS
  -> 冻结L1实现与2×2
      -> L1 STOP（实际落点）
          -> 归档MC-CVLRA，不进入北京/广州确认
```

L0只证明了低成本线性信号，不能替代神经结构特异性证据。L1已触发停止规则，后续确认路径作废。

## 8. 明确止损条件

- 冻结七变量按因果处理后的训练片缺失率超过10%，或出现违反 `timestamp<=t−1` 的历史回放可用性；
- L0 未达到相对 self 0.5%、recent 0.2% 且各4/5折同向；
- L1 未过 B0、B1、B2 或 B-flat 硬门；
- 2×2 的 `G_A|S=1` 不稳定，不能支持与锁定 ST 的组合价值；
- 任一L1训练种子的 M-shuffle 增益损失低于50%，或高污染 RMSE 恶化超过1.0%；
- outer/inner任一目标时间集合交集非空，或B-flat最小参数差超过3.0%；
- 需要解冻主干、改 checkpoint、扩大 lag/变量/损失/频率搜索或查看测试来挽救。

RES-Peak 不能在主候选失败后直接消费同一确认资源；需单独重做理论协议和资源登记。

## 9. 最终阶段结论

> **L1正式选择为STOP，MC-CVLRA按预注册规则止损归档。** L0线性信号没有转化为相对self-history、recent-only和等输入B-flat的稳定结构特异性优势；不得创建确认分支、打开正式test或通过扫描lag/变量/结构/损失挽救。该路线不是已成立的第二创新点。
