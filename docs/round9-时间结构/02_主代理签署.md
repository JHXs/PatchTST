# 主代理签署与审查记录（第 9 轮）

日期：2026-09-09。

## 候选

日历条件收缩先验残差预测（CCSPRF，原名 CEBRF）：训练期按 hour×weekday 唯一目标时间戳拟合收缩先验 `\tilde\mu_c=w_c\bar y_c+(1-w_c)\mu_0`，训练残差用 leave-one-out 先验，PatchTST 只拟合 `y-p`，推理输出 `p+\hat r`。0 额外可训练参数；与第一创新空间路径正交。

## 审查历史

- 设计代理（round9_designer，Codex gpt-5.6-luna high）初稿：CEBRF。
- 独立 reviewer（round9_reviewer，Codex gpt-5.6-luna high）首轮：方向不属频域/VMD/滤波/分解/多尺度换名，但阻断 5 项（target-encoding 自包含、EMB 逐步键与参数预算、permuted 双射对照无意义、命名过度、门单位不一致）。
- 设计代理修订后，reviewer 次轮：基本闭合，仍阻断 3 项（先验尺度、滑窗重复时间戳 LOO 去重、种子/bootstrap 聚合）。
- 设计代理终轮修订后，reviewer 终审 **PASS（仅限进入纯合成 L0 协议复核）**，明确该 PASS 不代表方法成立或真实收益。

## 主代理独立核验

- 泄漏闭合：日历键 hour(t+h)/weekday(t+h) 预测时可预知；先验只由训练唯一目标时间戳拟合；训练 LOO 排除 t 自身、n=1 时先验=μ_0；验证/测试用完整训练先验且不含测试目标；禁止按窗口样本去重。无前视。
- 归因闭合：Δ_centering=BASE→CCSPRF-global（中心化贡献），Δ_calendar=CCSPRF-global→CCSPRF（日历先验机制增量）。机制贡献≈0 即 STOP，避免 TACBR 复演。
- 门可执行：主效应门 median_s(1−RMSE(CCSPRF)/RMSE(BASE))≥5% 且逐种子改善；Δ_calendar≥2% 且 paired bootstrap 95% 不含 0；calendar-off 绝对值≤2% 且含 0；24→1 与 168→6 都要。
- 冻结完整：τ_L0=50、正式消融 τ∈{10,50,200,500}；L0 种子 20260939..20260968、正式 20260909..20260938、bootstrap RNG 20260969、B=10000；EMB d=4 共 124 参数、CCSPRF 0 参数；标准化目标尺度 + 反标准化。

## 主代理签署

**GO：仅进入纯合成 L0 协议与实现，不放行真实数据、不放行方法成立或真实收益。** 通过仅允许制定真实消融协议；任一门失败即 STOP，不调 τ/embedding 维度/主干挽救。合成资源消耗不返还，真实数据（含广州 9027 后 40%、B1 等）继续封存，本候选未消费。
