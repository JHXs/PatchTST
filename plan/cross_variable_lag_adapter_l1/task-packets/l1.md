# Task Packet：MC-CVLRA L1 实现与 smoke

- Scope：实现冻结 L1、修复reviewer P1的F10 BatchNorm/eval缺陷、替换两个CPU smoke，并完成168→6真实GPU batch512资源验收；不运行正式三种子选择。
- Files to read：`AGENTS.md`、理论 03/04/05、L0 源码/产物、`ST_PatchTST_model.py`、现有 ST 运行器/测试。
- Files allowed to edit：新增 L1 源码/测试/plan/report；仅在兼容性必要时小改 ST 模型；不得改 CSV、L0 权威结果或既有正式 ST 结果。
- Required skills：using-research-writing、paper-orchestration、experiment-results-planning、verification。
- Evidence/data inputs：北京全量时间戳；前 7008 行允许特征；训练片筛站/拟合；validation 早停与诊断。
- Required artifacts：数据模块、M/对照模块、运行器、复算器、逐阶段PatchTST状态审计、Git/源码追溯manifest、config/hash/ledger/log/prediction/diagnostic/checkpoint mapping、中文报告、测试、两个CPU `_smoke`和独立`_gpu_smoke`。
- Rejection checks：任意 test 数据构造；bfill；pool 不含1036；非训练期筛站/拟合；改变 P/K/lag/结构/门槛；F10任一PatchTST参数/buffer变化或任一子模块train；B-flat>3%；smoke 无后缀；dirty tree正式启动；正式目录或三种子产物出现。
- Validation commands：`uv run --frozen python -m py_compile *.py`；`uv run --frozen python -m unittest -v test_st_patchtst.py test_cross_variable_lag_l0.py test_cross_variable_lag_l1.py`；两个CPU任务smoke；168→6 GPU batch512资源smoke；独立复算；`git diff --check`。

当前资源状态：CPU与P1冻结链、真实AMD Radeon RX 7600 batch512资源smoke和严格联合复算均通过；正式启动门已关闭到仅剩clean-tree执行条件。
