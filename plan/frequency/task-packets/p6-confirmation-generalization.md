# P6 任务包：内部确认与外部泛化

> 状态：`not_entered`。F3与FA1均失败，没有可锁定的频域结构，本任务包仅保留历史规划，不得执行。

## 进入条件

- 原条件要求F3至少通过；实际F3和FA1均失败，因此不满足。
- 模型结构、损失、频带、阈值、训练预算和主比较在查看 2055–2059 结果前冻结。
- 外部目标在查看内部确认结果前写入决策记录和配置。

## 内部确认

- 北京 1013、168→6 为唯一主确认任务；24→1 完整报告但不替代主门。
- 种子固定为 2055–2059，不用这些种子继续搜索结构。
- 主对照为每个同种子的 `locked_st_clean`；加载同源锁定 ST 检查点。
- 报告逐种子原始值、配对效应、单侧精确符号检验、移动块 Bootstrap、逐预测步和峰值指标。
- 必做 `disable_frequency`、冻结参数、结果独立复算和输出完整性检查。

通过 F7 需同时满足：168→6 五个种子全部改善、单侧符号检验 p=0.03125、平均 RMSE 降幅至少 0.5%，且所有完整性检查通过。失败后降级为探索证据，不再换北京 1013 新种子搜索。

## 外部泛化

- 目标选择：预注册另一个北京中心站或广州中心站；不得按预实验最优挑选。
- 外部数据重新拟合训练期站点集、标准化器和权重，但不调整方法超参数。
- 使用同一锁定结构和 2055–2059 种子；主指标报告平均方向和分布，不要求复制北京的效应大小。
- 外部失败时严格限定单站结论，不修改内部确认结论。

## 输出位置

- `experiments/results/causal_frequency_ablation/p6_internal_confirmation/`
- `experiments/results/causal_frequency_ablation/p6_external_generalization/`

## 最终验收

结果目录满足协议契约；表格与图形只能由真实 CSV/JSON/预测生成；规范审查、质量审查和独立复算均通过后，才更新论文结论状态。
